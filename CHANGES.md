# 📝 Список изменений HomeOS

## ✅ Выполненные задачи

### 1. 🚫 Убраны халявные 600000 M$ при регистрации

**Файл:** [`shop.html`](shop.html:1214)
- Константа `INITIAL_BALANCE` изменена с `600000` на `0`
- Теперь новые пользователи регистрируются с нулевым балансом

---

### 2. 🔐 Добавлена смена пароля во всех приложениях

#### [`bank.html`](bank.html:260)
- Добавлена кнопка "Сменить пароль" в разделе Настройки
- Модальное окно с проверкой:
  - Старый пароль должен совпадать
  - Новый пароль нужно подтвердить
  - Минимум 3 символа
- Автосохранение в localStorage

#### [`shop.html`](shop.html:1003)
- Кнопка в header рядом с "Выйти"
- Полная валидация и автосохранение

#### [`my_info.html`](my_info.html:261)
- Кнопка в разделе "Действия"
- Интегрирована с системой модальных окон

#### [`my_work.html`](my_work.html:181)
- Кнопка в разделе "Настройки"
- Встроенная валидация паролей

---

### 3. ✏️ Добавлено редактирование товаров в магазине

**Файл:** [`shop.html`](shop.html:2139)

Новые функции:
- `editProduct(productId)` - открывает модальное окно редактирования
- `saveEditedProduct(productId)` - сохраняет изменения с валидацией
- `closeEditProductModal()` - закрывает модальное окно

Что можно редактировать:
- ✅ Название товара
- ✅ Описание
- ✅ Цена (M$)
- ✅ Количество на складе
- ✅ Иконка (эмодзи)
- ✅ Категория (Электроника, Одежда, Дом, Спорт)

Кнопки в интерфейсе "Мой магазин":
- **Редактировать** - открывает форму редактирования
- **Удалить** - удаляет товар с подтверждением

---

### 4. 🔒 Убрана автоматическая регистрация через Telegram

Изменены во всех файлах: [`bank.html`](bank.html:707), [`shop.html`](shop.html:1438), [`my_info.html`](my_info.html:422), [`my_work.html`](my_work.html:587)

**Изменение логики:**
- ❌ Раньше: при входе через Telegram пользователь создавался автоматически
- ✅ Теперь: только проверка существования пользователя
- Если пользователя нет - остается на экране входа/регистрации
- Нужно зарегистрироваться вручную

**Функция `tryTelegramAutoLogin()`:**
```javascript
// Старая версия - создавала пользователя
if (!user) {
  user = { /* новый пользователь */ };
  users.push(user);
  saveUsers(users);
}

// Новая версия - только проверка
if (!user) {
  return false; // Пользователя нет, показываем форму входа
}
```

---

### 5. 💾 Автосохранение данных

Все действия автоматически сохраняются в `localStorage` браузера:

**В [`bank.html`](bank.html:400):**
- После каждого перевода → `setUsers(users)`, `setHistory(h)`
- После начисления → `setUsers(users)`, `setHistory(h)`, `setNotes(notes)`
- После смены пароля → `setUsers(users)`, `setCurrent(user)`

**В [`shop.html`](shop.html:1840):**
- После покупки → `saveProducts()`, `saveStores()`, `saveUsers()`
- После добавления товара → `saveProducts()`
- После редактирования товара → `saveProducts()`
- После смены пароля → `saveUsers()`, обновление сессии

**В [`my_info.html`](my_info.html:538):**
- После сохранения паспорта → `setRecs(all)`
- После сохранения контактов → `setRecs(all)`
- После загрузки файлов → `setRecs(all)`
- Логирование в аудит → `setAudit(arr)`

**В [`my_work.html`](my_work.html:496):**
- После завершения смены → `setShifts(all)`, `setRunning(r)`
- После изменения чек-листа → `setTasks(map)`
- После регистрации → `setUsers(list)`

---

### 6. 🎨 Оптимизация для мобильных устройств

**Файл:** [`index.html`](index.html:9)

#### Темная тема (черный фон)
- Основной фон: `#000000` → `#0a0a0a` → `#111111`
- Синие/фиолетовые акценты для глубины
- Улучшенные тени и контрасты

#### Мобильная адаптация
```css
@media (max-width: 480px) {
  .phone { width: 100vw; border-radius: 0; } /* Полный экран */
  .grid { gap: 14px; } /* Компактные отступы */
  .icon { width: 95px; height: 95px; } /* Меньшие иконки */
}
```

**Улучшения:**
- 📱 Полноэкранный режим на мобильных
- 🎯 Оптимизированные размеры кнопок и иконок
- 💫 Плавные анимации и переходы
- 🌈 Свечение иконок при наведении

---

## 🤖 Документация по настройке бота

Созданы файлы:
- [`bot.py`](bot.py:1) - готовый Python скрипт для Telegram бота
- [`BOT_SETUP.md`](BOT_SETUP.md:1) - подробная инструкция по настройке

**Два способа добавить кнопку "Запустить":**
1. ⭐ **БЕЗ КОДА** - через @BotFather (рекомендуется)
2. 💻 **С КОДОМ** - Python бот с автоматическими кнопками

---

## 🔧 Технические детали

### Структура данных localStorage

**Банк (bank.html):**
- `msbank_users_v11` - пользователи с балансами
- `msbank_history_v11` - история переводов
- `msbank_notes_v11` - уведомления для пользователей
- `msbank_session_v11` - текущая сессия

**Магазин (shop.html):**
- `mtshop_users` - пользователи магазина
- `mtshop_products` - товары
- `mtshop_stores` - магазины владельцев
- `mtshop_withdrawals` - запросы на вывод средств
- `mtshop_session` - текущая сессия
- `mtshop_notifications` - уведомления

**Моя информация (my_info.html):**
- `myinfo_users_v1` - пользователи
- `myinfo_records_v1` - записи (паспорт, контакты и т.д.)
- `myinfo_audit_v1` - журнал действий
- `myinfo_session_v1` - текущая сессия

**Моя работа (my_work.html):**
- `mywork_users_v2` - пользователи
- `mywork_session_v2` - текущая сессия
- `mywork_shifts_v2` - история смен
- `mywork_tasks_v2` - состояние чек-листов
- `mywork_running_v1` - активные смены

### Безопасность
- ✅ Все пароли хранятся в открытом виде (для демо)
- ✅ Валидация всех форм перед сохранением
- ✅ Проверка прав доступа (админ/пользователь)
- ✅ Защита от дубликатов имен пользователей

---

## 📊 Статистика изменений

- **Файлов изменено:** 5 (index.html, bank.html, shop.html, my_info.html, my_work.html)
- **Файлов создано:** 2 (bot.py, BOT_SETUP.md)
- **Добавлено функций:** 8 (смена пароля × 4, редактирование товаров × 4)
- **Убрано автосоздания:** 4 файла (отключена регистрация через Telegram)

---

## 🚀 Что дальше?

1. **Разместите на хостинге** (GitHub Pages, Vercel, Cloudflare Pages)
2. **Создайте Telegram бота** через @BotFather
3. **Настройте кнопку запуска** (инструкция в BOT_SETUP.md)
4. **Протестируйте** все функции в Telegram

Все готово к использованию! 🎉